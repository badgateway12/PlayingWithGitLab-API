/**
 * Given a string, find the first repeated character in it. Return null if no such character is found.
 * Example: "abca" = 'a', "awsd" = null, "abssb" = 's'
 * Assume, we have lowercase letters only.
 */
public class Task {
    public static Character firstRepeatingCharacter(String input)
    {
        return null;
    }
}
